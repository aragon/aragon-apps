const modalRoot = document.createElement('div')
modalRoot.setAttribute('id', 'modal-root')
modalRoot.dataset.testid = 'modal-root'
document.body.appendChild(modalRoot)

const editAllocation = document.createElement('div')
editAllocation.setAttribute('id', 'edit-allocation')
editAllocation.dataset.testid = 'edit-allocation'
document.body.appendChild(editAllocation)
