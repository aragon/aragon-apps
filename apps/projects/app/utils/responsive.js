const REM_SIZE = parseFloat(getComputedStyle(document.documentElement).fontSize)
const BASE_CARD_WIDTH = 249
const CARD_STRETCH_BREAKPOINT = 2 * BASE_CARD_WIDTH + 2 * 2 * REM_SIZE + 2 * 2 * REM_SIZE

export { BASE_CARD_WIDTH, CARD_STRETCH_BREAKPOINT }