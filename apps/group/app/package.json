{
  "name": "group",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@aragon/ui": "^0.3.0",
    "immutability-helper": "^2.5.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-motion": "^0.5.2",
    "styled-components": "^2.2.3"
  },
  "devDependencies": {
    "react-scripts": "1.0.16",
    "sync-files": "^1.0.3"
  },
  "scripts": {
    "start": "npm run sync-assets && react-scripts start",
    "build": "npm run sync-assets && react-scripts build",
    "test": "npm run sync-assetes && react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sync-assets": "scripts/sync-assets.js"
  }
}
